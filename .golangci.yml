version: "2"

linters:
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable: []

issues:
  exclude-rules:
    # defer Close is idiomatic Go — the error cannot be handled
    - linters: [errcheck]
      text: "Error return value of `.*\\.Close` is not checked"
    - linters: [errcheck]
      text: "Error return value of `os\\.(Setenv|Unsetenv)` is not checked"
    - linters: [errcheck]
      text: "Error return value of `defer"
